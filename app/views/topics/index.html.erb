<h1>What's your favorite pet to put in a hat?</h1>

<img src="http://i.imgur.com/MQHYB.jpg" height="500"/>

<table>
  <thead>
    <tr>
      <th>Let's Vote!</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.title, topic_path(topic) %></td>
        <td><%= pluralize(topic.votes.count, "vote") %></td>
        <td><%= button_to '+1', upvote_topic_path(topic), method: :post %></td>
        <td><%= button_to '-1', downvote_topic_path(topic), method: :post %></td>
        <td><%= link_to 'Delete', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<img src="http://www.laurabucur.ro/wp-content/uploads/2009/06/dog-hat.jpg" height="500" />

<br>

<%= link_to 'New Topic', new_topic_path %>
<%= link_to 'About Page', about_topics_path %>
